# Supabase local dev (optional). Install: brew install supabase/tap/supabase
# Run: supabase link --project-ref YOUR_REF && supabase db push

project_id = "cadence"

[api]
enabled = true
port = 54321

[auth]
enabled = true
enable_anonymous_sign_ins = true

[auth.external.apple]
enabled = true
